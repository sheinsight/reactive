# Installation \{#installation}

Install Reactive with your preferred package manager.

import { PackageManagerTabs } from 'rspress/theme'

<PackageManagerTabs
  command={{
    npm: 'npm install @shined/reactive',
    yarn: 'yarn add @shined/reactive',
    pnpm: 'pnpm add @shined/reactive',
    bun: 'bun add @shined/reactive',
  }}
/>

## Next Steps \{#next-steps}

Reactive is designed to be framework-agnostic. Choose the guide that fits your needs:

- [Using it with ⚛️ React](/guide/usage/react)
- [Using it with 🟡 Vanilla JavaScript](/guide/usage/vanilla)
